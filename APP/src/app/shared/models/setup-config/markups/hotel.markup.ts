export interface HotelMarkup {
    markup_hotel_id: number,
    markup_id: number,
    markup_mode: string,
    markup: number,
    min_markup: number,
    max_markup: number,
    markup_type: string
}