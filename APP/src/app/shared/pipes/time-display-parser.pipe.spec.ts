import { TimeDisplayParserPipe } from './time-display-parser.pipe';

describe('TimeDisplayParserPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeDisplayParserPipe();
    expect(pipe).toBeTruthy();
  });
});
