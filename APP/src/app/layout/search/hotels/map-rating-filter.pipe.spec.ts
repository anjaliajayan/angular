import { MapRatingFilterPipe } from './map-rating-filter.pipe';

describe('MapRatingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MapRatingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
