import { AncillariesPipePipe } from './ancillaries-pipe.pipe';

describe('AncillariesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AncillariesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
