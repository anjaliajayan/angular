import { MealPlanFilterPipe } from './meal-plan-filter.pipe';

describe('MealPlanFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MealPlanFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
