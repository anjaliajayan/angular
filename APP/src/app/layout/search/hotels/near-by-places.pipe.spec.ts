import { NearByPlacesPipe } from './near-by-places.pipe';

describe('NearByPlacesPipe', () => {
  it('create an instance', () => {
    const pipe = new NearByPlacesPipe();
    expect(pipe).toBeTruthy();
  });
});
