import { SearchByRatingPipe } from './search-by-rating.pipe';

describe('SearchByRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
