import { TripadvisorPipe } from './tripadvisor.pipe';

describe('TripadvisorPipe', () => {
  it('create an instance', () => {
    const pipe = new TripadvisorPipe();
    expect(pipe).toBeTruthy();
  });
});
