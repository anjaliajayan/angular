import { NearbyfilterPipe } from './nearbyfilter.pipe';

describe('NearbyfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NearbyfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
