import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { NewGroupProfileComponent } from './new-group-profile.component';

describe('NewGroupProfileComponent', () => {
  let component: NewGroupProfileComponent;
  let fixture: ComponentFixture<NewGroupProfileComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ NewGroupProfileComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(NewGroupProfileComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
